window.addEventListener('DOMContentLoaded', function (event) {




    const pegaEscolhaUsuario = document.getElementById('pegaEscolhaUsuario');
    const opcoes = ['pedra', 'papel', 'tesoura', 'lagarto', 'spock'];
    const resultado = document.getElementById('resultado');

    pegaEscolhaUsuario.addEventListener('click', function ())



    const computador = choises[Math.floor(Math.random() * opcoes.length)];
    const pegaEscolhaUsuario = document.getElementById('escolhaUsuario').value;
    switch(escolhaUsuario) {
        case 'pedra':
            // opcoes[0]
            if (escolhaUsuario == computador) {
                resultado.innerHTML = `Empate, ambos escolheram pedra.`
            // console.log('empate')
        }   else if (escolhaUsuario == 'tesoura' || escolhaUsuario == 'lagarto'){
            resultado.innerHTML = `O computador escolheu` $

        }

        break:
        default:

    }







})